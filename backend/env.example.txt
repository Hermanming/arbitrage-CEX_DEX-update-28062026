# =============================================================================
# Backend Environment Variables (CEX-DEX Arbitrage Bot)
# Copy this file to `.env` and fill in real values. Never commit `.env`.
# =============================================================================

# MongoDB connection — local install or MongoDB Atlas connection string
# Example local:  mongodb://localhost:27017
# Example Atlas:  mongodb+srv://USER:PASS@cluster0.xxxxx.mongodb.net
MONGO_URL=mongodb://localhost:27017

# Database name (will be created automatically)
DB_NAME=arbitrage_bot

# CORS — allow any origin during dev. In prod, set to your frontend URL only.
CORS_ORIGINS=*

# Fernet symmetric encryption key (32-byte URL-safe base64).
# Generate ONCE and keep this safe — used to encrypt API keys & private keys in MongoDB.
# Generate with:
#     python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
# If you lose this key, encrypted credentials in MongoDB cannot be recovered.
FERNET_KEY=replace_me_with_a_fresh_fernet_key

# Solana RPC endpoint used for Jupiter swaps & balance queries.
# Free public:  https://api.mainnet-beta.solana.com  (rate-limited)
# Recommended:  Helius / QuickNode / Alchemy paid RPC for production
SOLANA_RPC=https://api.mainnet-beta.solana.com
