# Minimal runtime requirements for Arbitrage Bot
# For self-host VPS deployment (Windows/Linux), Python 3.11.x recommended
# Last updated: 2026-02-XX

# === Core web framework ===
fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
python-dotenv>=1.0

# === Data validation ===
pydantic>=2.5,<3.0
email-validator>=2.0

# === MongoDB driver ===
motor>=3.5
pymongo>=4.6

# === HTTP / WebSocket ===
httpx>=0.27
websockets>=12.0

# === Crypto / Encryption ===
cryptography>=42.0

# === Binance API (CEX leg) ===
python-binance>=1.0.20

# === Solana / Jupiter (DEX leg) — version-pinned for ABI compatibility ===
solana==0.40.0
solders==0.27.1
base58>=2.1
