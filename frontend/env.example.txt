# =============================================================================
# Frontend Environment Variables (CEX-DEX Arbitrage Bot)
# Copy this file to `.env` and fill in real values. Never commit `.env`.
# =============================================================================

# Public backend URL the React app calls.
# Local dev:     http://localhost:8001
# VPS deploy:    http://<your-vps-public-ip>:8001 or https://api.yourdomain.com
REACT_APP_BACKEND_URL=http://localhost:8001

# Disable websocket overlay for faster CRA dev startup (optional)
WDS_SOCKET_PORT=443
